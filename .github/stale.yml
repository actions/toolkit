# Configuration for probot-stale - https://github.com/probot/stale

# Number of days of inactivity before an Issue or Pull Request becomes stale
daysUntilStale: 60

# Number of days of inactivity before an Issue or Pull Request with the stale label is closed.
# Set to false to disable. If disabled, issues still need to be closed manually, but will remain marked as stale.
daysUntilClose: 7

# Only issues or only pull requests can be limited by using the only option.
# Can be 'issues' or 'pulls'. If not specified, both will be limited.
# only: issues

# Label to use when marking as stale
staleLabel: stale

# Comment to post when marking as stale. Set to `false` to disable
markComment: >
  This issue has been automatically marked as stale because it has not had
  recent activity. It will be closed if no further activity occurs. Thank you
  for your contributions.

# Comment to post when removing the stale label.
# unmarkComment: >
#   Your comment here.

# Comment to post when closing a stale Issue or Pull Request.
closeComment: >
  This issue has been automatically closed because it has not had recent
  activity. If you believe this is still relevant, please reopen it or create
  a new issue with updated information.

# Limit the number of actions per hour, from 1-30. Default is 30
limitPerRun: 30

# Limit to only `issues` or `pulls`
# only: issues

# Optionally, specify configuration settings that are specific to just 'issues' or 'pulls':
# pulls:
#   daysUntilStale: 60
#   markComment: >
#     This pull request has been automatically marked as stale because it has not had
#     recent activity. It will be closed if no further activity occurs. Thank you
#     for your contributions.

# issues:
#   daysUntilStale: 60
#   markComment: >
#     This issue has been automatically marked as stale because it has not had
#     recent activity. It will be closed if no further activity occurs. Thank you
#     for your contributions.

# Exempt certain labels from being marked as stale
exemptLabels:
  - "pinned"
  - "security"
  - "priority: high"
  - "breaking change"

# Exempt all issues/PRs with milestones from being marked as stale
exemptMilestones: true

# Exempt all issues/PRs with assignees from being marked as stale
exemptAssignees: false

# Exempt all issues/PRs with projects from being marked as stale
exemptProjects: false
